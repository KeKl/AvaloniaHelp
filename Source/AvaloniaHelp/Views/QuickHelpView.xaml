<UserControl x:Class="AvaloniaHelp.Views.QuickHelpView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Properties="clr-namespace:AvaloniaHelp.Properties;assembly=AvaloniaHelp"
             xmlns:Controls="clr-namespace:AvaloniaHelp.Controls;assembly=AvaloniaHelp">

  <TabControl Grid.Column="0">
    
    <TabItem Header="{Binding Title, Mode=TwoWay}">
      <ContentControl Content="{Binding ActiveContent}" />
    </TabItem>
    
  </TabControl>

</UserControl>